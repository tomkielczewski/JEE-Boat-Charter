/**
 * JSF view model classes. In order not to use entity classes in the view layer the model classes are introduced. Only
 * model classes can be use to generate http responses and only model classes can be updated by http requests.
 */
package pl.edu.pg.eti.kask.boatcharter.boat.model;
