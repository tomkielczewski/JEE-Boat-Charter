/**
 * Classes responsible for supporting push mechanism. Push mechanism can be implemented using WebSockets protocol which
 * allows backend server to push subsequent notifications using WebSocket protocol (which is different than HTTP
 * protocol).
 */
package pl.edu.pg.eti.kask.boatcharter.push;
